@model Shopping.Data.Entities.City

@{
    ViewData["Title"] = "Delete";
}

<h1>Deletar</h1>

<h3>Tem certeza que deseja deletar essa Cidade?</h3>
<div>
    <partial name="PartialViews/_CityDetails" />
</div>
    <div>
    <form asp-action="DeleteCity">
        <input type="hidden" asp-for="IdCity" />
        <input type="submit" value="Deletar" class="btn btn-outline-danger" /> |
        <a asp-action="DetailsState" asp-route-id="@Model?.State.IdState" class="btn btn-outline-success">Voltar</a>
    </form>
</div>
