@model Shopping.Data.Entities.City

@{
	ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
	<partial name="PartialViews/_CityDetails" />
</div>
<div>
	<a asp-action="EditCity" asp-route-id="@Model?.IdCity" class="btn btn-outline-warning">Editar</a>
	<a asp-action="DetailsState" asp-route-id="@Model?.State.IdState" class="btn btn-outline-success">Voltar</a>
</div>